name: VM State Change

on:
  workflow_dispatch:
    inputs:
      STATE:
        required: true
        type: choice
        options:
          - NONE
          - START
          - STOP
          - RESTART

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: login to ACR
        id: aacr
        run: |
          # Your JSON data
          json_data={
            "name": "zs-oyo-temp-vm",
            "privateIp": "10.73.100.4",
            "resourceGroup": "Oyo-migration-RG"
          }

          echo $json_data
  
          echo "::set-output name=json_data::$json_data"
      - run: |
          $(echo "${{ steps.aacr.outputs.json_data }}")
     
