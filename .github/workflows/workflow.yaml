name: test workflow

on:
  workflow_dispatch:
    inputs:
      PRODUCT_JIRA_TICKETS:
        required: true
        type: string
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: python test1.py
